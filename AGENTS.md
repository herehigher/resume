# 開発規約

## プロジェクト方針

- 個人利用を前提とした、日本語の履歴書・職務経歴書作成アプリです。
- 入力データはローカルで扱い、外部へ送信しません。
- シンプルさを優先し、依存パッケージやビルド工程は追加しません。
- 新しい仕組みを導入する場合は、利用者から明示的な要望があるときだけ検討します。

## ファイルの役割

- `index.html`: 実際に利用・配布する単一ファイル版
- `styles.css`: CSSの編集用ソース
- `app.js`: JavaScriptの編集用ソース
- `README.md`: 利用方法と概要

## 実装ルール

1. `styles.css` と `app.js` を変更したら、`index.html` 内のCSS・JavaScriptも必ず同期する。
2. `index.html` は `file://` から直接開いて動作する状態を維持する。
3. CDN、外部フォント、外部API、アクセス解析を追加しない。
4. 個人情報をログ出力したり、ネットワークへ送信したりしない。
5. ローカルストレージの既存データを壊さないよう、新しい項目には初期値を設定する。
6. ユーザー入力をHTMLへ表示するときは必ずエスケープする。リンクは `http://` または `https://` のみクリック可能にする。
7. デスクトップとスマートフォンの両方で操作できる状態を維持する。
8. PDF用レイアウトはA4サイズを基準にし、改ページ・文字切れ・はみ出しを確認する。
9. 画面上の文言とコードコメントは、原則として日本語で簡潔に書く。

## 変更後の確認

- `node --check app.js` が成功すること
- `index.html` 内のJavaScriptにも構文エラーがないこと
- 履歴書と職務経歴書を切り替えられること
- 入力内容がプレビューへ反映されること
- オンラインプロフィールのURLが履歴書・職務経歴書でクリックできること
- 学歴・職歴・免許・資格の未入力行がPDFへ表示されないこと
- 下書きの保存・再読み込み・ページ更新後の復元が動作すること
- 入力例の表示・編集で保存済み下書きが上書きされないこと
- Chromeで `index.html` を直接開いて表示が崩れないこと
- 印刷プレビューでA4 PDFのレイアウトを確認すること

## 変更範囲

- 依頼と無関係なデザインや入力データは変更しない。
- 保存データを削除する変更や互換性を失う変更は、事前に利用者へ確認する。
