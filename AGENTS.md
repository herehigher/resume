# 開発規約

## プロジェクト方針

- 個人利用を前提とした、日本語の履歴書・職務経歴書作成Webアプリです。
- 正式な実行・配布環境は GitHub Pages とし、`file://` での直接実行は対象外です。
- 入力データはブラウザ内で扱い、外部API、アクセス解析、外部フォントへ送信しません。
- 保守しやすいモジュール構成を優先します。公開用ファイルは `site/` にまとめます。
- 本番コードはブラウザ標準のHTML、CSS、ES Modulesで構成し、実行時フレームワークやビルドを必須にしません。
- テスト専用の開発依存関係は、目的と更新方法が明確な場合に限り追加できます。

## ディレクトリの役割

- `site/index.html`: GitHub Pages が配信するアプリのHTMLシェル
- `site/assets/js/`: 状態、UI、テンプレート、共通処理
- `site/assets/css/`: 基本、編集画面、レスポンシブ、印刷、書類テンプレート
- `tests/`: Node単体テストと、今後追加するブラウザ・PDFテスト
- `.github/workflows/`: CIとTagベースのPagesデプロイ
- `README.md`: 公開利用者向けドキュメント

## 実装ルール

1. ES Modulesは責務ごとに分割し、循環依存を作らない。
2. `site/` はビルドなしで静的Webサーバーから配信できる状態を維持する。
3. CDN、外部フォント、外部API、アクセス解析を追加しない。
4. 個人情報をログ出力したり、ネットワークへ送信したりしない。
5. 保存形式は `resume-studio-web-v1` を使用する。旧版データの移行・互換処理は追加しない。
6. ユーザー入力をHTMLへ表示するときは必ずエスケープする。リンクは `http://` または `https://` のみクリック可能にする。
7. デスクトップとスマートフォンの両方で操作できる状態を維持する。
8. PDF用レイアウトはA4を基準に確認し、文字切れ・はみ出しを防ぐ。
9. 画面上の文言とコードコメントは、日本語で簡潔に書く。

## 変更後の確認

- `npm test` が成功すること
- 全JavaScriptモジュールに構文エラーがないこと
- 静的Webサーバーから `site/` を開けること
- 日本語の履歴書と職務経歴書を切り替えられること
- 入力内容がプレビューへ反映されること
- オンラインプロフィールのURLがクリックできること
- 学歴・職歴・免許・資格の未入力行がPDFへ表示されないこと
- 入力例の表示・編集で保存済み下書きが上書きされないこと
- ネットワークへ個人データを送信しないこと

## 変更範囲

- 依頼と無関係なデザインや入力データは変更しない。
- v0.1以前の `resume-studio-data-v1` は移行対象外とし、読み込み・削除を行わない。
- 保存データを意図的に削除する変更は、事前に利用者へ確認する。
