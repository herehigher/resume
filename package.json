{
  "name": "resume-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-site.mjs",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "test": "node --test tests/*.test.js && npm run check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11"
  }
}
